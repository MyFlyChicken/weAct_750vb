# bootloader/boards/weact_stm32h750_mini.conf
# Board-specific MCUboot configuration
# Author: MyFlyChicken

# 使用 QSPI Flash
CONFIG_FLASH=y
CONFIG_SPI=y
CONFIG_SPI_NOR=y

# 使用 UART3 作为控制台
CONFIG_UART_CONSOLE=y
CONFIG_PRINTK=y

# 禁用不需要的外设
CONFIG_I2C=n
CONFIG_USB_DEVICE_STACK=n

